import React from 'react';

const SelectProduct = ({ cart }) => {
    const { name, img } = cart
    return (
        <div>
            <h1>{name}</h1>

            <div>

            </div>
        </div>
    );
};

export default SelectProduct;